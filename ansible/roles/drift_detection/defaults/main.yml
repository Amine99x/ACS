---
rancher_kubeconfig: "/etc/rancher/rke2/rke2.yaml"
kubectl_binary: "/usr/local/bin/kubectl"

drift_expected_clusters:
  - kubecloud-dev
  - kubecloud-prod

drift_expected_fleet_clusters: 2

drift_expected_status: "True"

drift_report_enabled: true

drift_policy_file: "{{ playbook_dir }}/../policies/kubecloud-policy.yml"

# Detection only. No remediation is performed here.
remediation_dry_run: true
