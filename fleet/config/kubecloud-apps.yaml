apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: kubecloud-applications
  namespace: fleet-default
spec:
  repo: "https://github.com/Amine99x/ACS.git"
  branch: main
  paths:
    - fleet/applications
